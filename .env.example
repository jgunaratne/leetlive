# ── Gemini Auth (pick ONE) ────────────────────────────────────────────────
# Option 1 — API key
GEMINI_API_KEY=

# Option 2 — Vertex AI via ADC (run: gcloud auth application-default login)
# GOOGLE_CLOUD_PROJECT=
# GOOGLE_CLOUD_LOCATION=us-central1

# ── Server ───────────────────────────────────────────────────────────────
# PORT=3000
